import { ArticlesTransport } from './ArticlesTransport';

describe('ArticlesTransport', () => {
  const mockClass = new ArticlesTransport();

  it('should be defined', () => {
    expect(mockClass).toBeDefined();
  });

  it('should get all articles', () => {
    expect(mockClass.getAll).toBeDefined();
  });

  it('should get one article', () => {
    expect(mockClass).toBeDefined();
  });

  it('should create an articles', () => {
    expect(mockClass).toBeDefined();
  });

  it('should update an articles', () => {
    expect(mockClass).toBeDefined();
  });

  it('should delete an articles', () => {
    expect(mockClass).toBeDefined();
  });
});
